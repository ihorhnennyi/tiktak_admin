import { Container, Typography } from "@mui/material";
export default function Home() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h5">Admin — вы вошли</Typography>
    </Container>
  );
}
